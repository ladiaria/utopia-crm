# Less/more debug in testing time
DEBUG_TESTS = False
DEBUG_PRODUCTS = False

# Change to True if you want to make the first test (according to runtests.sh) fail
# TESTS_FIRST_TEST_SHOULD_FAIL = False

# Useful if you have a configured smtp local testing tool to have a look on all emails generated
# TEST_EMAIL_BACKEND = "django.core.mail.backends.smtp.EmailBackend"
